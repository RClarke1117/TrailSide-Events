---
interface Props {
  title: string;
  texture: string;
  className?: string;
  image?: string;
  alt?: string;
  credit?: string;
  captionOnly?: boolean;
}

const { title, texture, className = '', image = '', alt = '', credit = '', captionOnly = false } = Astro.props;
const photo = image.trim();
---

<figure class:list={['frame', className]} data-reveal>
  <div class="frame-mat">
    {
      photo ? (
        <img src={photo} alt={alt || title} />
      ) : (
        <div class:list={['texture', texture]}></div>
      )
    }
    {!photo && <p class="placeholder-label">Venue photo coming</p>}
  </div>
  <figcaption>
    <strong>{title}</strong>
    {!captionOnly && <span>{photo ? credit || 'Venue' : 'Venue photo coming'}</span>}
  </figcaption>
</figure>

<style>
  .frame-mat img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .frame-credit {
    margin: -0.35rem 0 0;
    color: var(--ink-soft);
    font-size: 0.75rem;
  }
</style>
